import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    text: {
        fontSize: 16,
        alignSelf: 'center',
        color: '#ffffff',
    },
    wrapperCustom: {
        borderRadius: 5,
        padding: 6,
        minWidth: 100,
    },
});
